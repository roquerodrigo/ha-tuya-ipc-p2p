# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Sao_Paulo"
    groups:
      github-actions:
        patterns:
          - "*"

    commit-message:
      prefix: "deps"
      include: "scope"

  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "America/Sao_Paulo"
    groups:
      python-deps:
        patterns:
          - "*"
        exclude-patterns:
          - "homeassistant"
    ignore:
      # Dependabot should not update Home Assistant — that pin tracks the
      # ``homeassistant`` key in hacs.json (set manually).
      - dependency-name: "homeassistant"
    commit-message:
      prefix: "deps"
      prefix-development: "deps-dev"
      include: "scope"
